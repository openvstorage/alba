language: c
cache: apt
matrix:
  include:
  - os: linux
    env: SUITE=build
  - os: linux
    env: SUITE=system2

sudo: required
before_install: ./travis.sh before_install
install: ./travis.sh install
script: ./travis.sh script
git:
    submodules: false
